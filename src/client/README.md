# Browser boundary

Browser code may render normalized contracts and the promoted field projection. It must not import SQLite, raw evidence, server credentials, or vendor-specific source adapter logic.
