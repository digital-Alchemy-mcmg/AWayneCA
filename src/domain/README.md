# Domain boundary

The reconciled, versioned Property Envelope contract belongs here after COMPILER-001 passes. UI, persistence, promotion, Firestore projection, and export must import that contract; none may redefine it.
