# Source adapters

Production source adapters belong here after their contract packet opens. They must implement the fetch, validate, preserve, normalize, timestamp, health, drift, fail-safe, and escalation lifecycle in the Build Contract.
