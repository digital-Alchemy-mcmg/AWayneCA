# Promotion boundary

The explicit desktop-to-mobile Promotion Gate belongs here. Firestore writes must occur only behind this boundary and may publish only the validated, bounded field projection of the canonical Property Envelope.
